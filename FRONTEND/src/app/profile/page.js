import ProfileContainer from "@/components/profile/ProfileContainer";
import React from "react";

const page = () => {
  return (
    <div>
      <ProfileContainer />
    </div>
  );
};

export default page;
